# MyDhobi

This repository contains two applications:

- `web/` — the Next.js website deployed at https://mydhobi.vercel.app/
- `app/` — the Expo Android application

## Website

```bash
npm run dev
npm run build
```

## Android app

```bash
npm run app:start
npm run app:android
npm run app:build-apk
```
